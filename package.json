{
  "name": "bri-redesign",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "start:all": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "lint:all": "npm --prefix backend run lint && npm --prefix frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}

# Production Environment Variables for Azure App Service
# Set these in Azure Portal > App Service > Configuration > Application Settings

# Server Configuration
PORT=8080
NODE_ENV=production

# MongoDB Atlas Connection
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/brimo_db?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-strong-secret-key-change-this-in-production-minimum-64-characters
JWT_EXPIRES_IN=7d

# Password Hashing
BCRYPT_ROUNDS=10

# API Configuration
API_TIMEOUT=15000

# CORS Configuration
# Add your Vercel frontend URL here
CORS_ORIGIN=https://your-app.vercel.app

# Stock API Settings
STOCK_CACHE_TIME=300000
STOCK_FALLBACK_ENABLED=true
